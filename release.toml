[project]
id = "mousr"
name = "Mousr"
version_file = "VERSION"
description = "Keyboard-driven mouse control for Wayland"
homepage = "https://github.com/toxdes/mousr"
repository = "https://github.com/toxdes/mousr"
license = "MIT"
maintainer = "toxdes <hi@toxdes.com>"

[build]
dockerfile = "Dockerfile"
context = "."
output_dir = "dist"
platforms = ["linux/amd64", "linux/arm64"]

[build.args]
BUILD = "release"
INCLUDE_APPIMAGE = "0"

[artifacts]
deb = ["*.deb"]
rpm = ["*.rpm"]
archive = ["*.tar.gz"]

[package]
binary = "mousr"

[deb]
package_name = "mousr"
section = "utils"
priority = "optional"
architectures = ["amd64", "arm64"]
depends = [
  "libwayland-client0",
  "libxkbcommon0",
]

[rpm]
package_name = "mousr"
release = "1"
architectures = ["x86_64", "aarch64"]
requires = [
  "wayland-libs",
  "libxkbcommon",
]

[aur]
binary_package = "mousr-bin"
git_package = "mousr-git"
architectures = ["x86_64", "aarch64"]
depends = [
  "wayland",
  "libxkbcommon",
]
git_pkgbuild = "PKGBUILD"

[hosting]
public_base_url = "https://packages.toxdes.com"
apt_prefix = "apt"
rpm_prefix = "rpm"
release_prefix = "releases"

[hosting.apt]
suite = "stable"
component = "main"
origin = "toxdes"
label = "Toxdes"

[hosting.signing]
gpg_key_env = "GPG_KEY_ID"
gpg_passphrase_env = "GPG_PASSPHRASE"

[hosting.aur]
host = "aur.archlinux.org"
ssh_user = "aur"
maintainer = "toxdes <hi@toxdes.com>"
